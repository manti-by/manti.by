import os
import raven

from .base import *


SECRET_KEY = ''

DEBUG = True

COMPRESS_ENABLED = False

TEMPLATE_DEBUG = False

ALLOWED_HOSTS = ['127.0.0.1', 'manti.loc',
                 'ru.manti.loc', 'en.manti.loc']

BASE_URL = 'http://manti.loc'

SESSION_COOKIE_DOMAIN = '.manti.loc'

STATIC_URL = 'https://manti.loc/static/'
STATIC_ROOT = '/srv/manti.by/static'

MEDIA_URL = 'https://manti.loc/content/'
MEDIA_ROOT = '/srv/manti.by/content'

LOCALE_URLS = {
    'be': 'manti.loc',
    'ru': 'ru.manti.loc',
    'en': 'en.manti.loc',
}

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'manti',
        'USER': 'manti',
        'PASSWORD': 'pa55word',
        'HOST': 'manti-by-postgres',
        'PORT': '',
    }
}

CELERY_BROKER_URL = 'redis://manti-by-redis:6379/0'

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
        'LOCATION': 'manti-by-memcached:11211',
    }
}

LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'file_app': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': '/var/log/manti.by/app.log',
        },
        'file_django': {
            'level': 'DEBUG',
            'class': 'logging.FileHandler',
            'filename': '/var/log/manti.by/django.log',
        },
        'console': {
            'level': 'DEBUG',
            'class': 'logging.StreamHandler',
        },
        'null': {
            'class': 'logging.NullHandler',
        },
    },
    'loggers': {
        '': {
            'handlers': ['console', 'file_app'],
            'level': 'WARNING',
            'propagate': True,
            'formatter': 'verbose',
        },
        'django': {
            'handlers': ['file_django'],
            'level': 'WARNING',
            'propagate': True,
            'formatter': 'simple',
        },
        'django.template': {
            'handlers': ['null'],
        },
        'django.db.backends': {
            'handlers': ['null'],
        },
    },
}

INSTAGRAM_CLIENT_ID = ''
INSTAGRAM_ACCESS_TOKEN = ''
INSTAGRAM_CLIENT_SECRET = ''

RAVEN_CONFIG['dsn'] = ''
