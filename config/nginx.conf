log_format stats '[$time_local] $request_uri';

upstream manti-by {
    server 127.0.0.1:8080;
}

server {
    if ($host = manti.by) {
        return 301 https://$host$request_uri;
    }

    listen 80;
    server_name manti.by;
    return 404;
}

server {
    if ($host = www.manti.by) {
        return 301 https://$host$request_uri;
    }

    listen 80;
    server_name www.manti.by;
    return 301 https://manti.by$request_uri;
}

server {
    listen 443 ssl http2;
    server_name www.manti.by;

    ssl_certificate /etc/letsencrypt/live/www.manti.by/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/www.manti.by/privkey.pem;

    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    return 301 https://manti.by$request_uri;
}

server {
    listen 443 ssl http2 default_server;
    server_name manti.by;
    charset     utf-8;

    client_max_body_size 500M;

    access_log   /mnt/nostromo/www/manti.by/log/nginx_access.log stats;
    error_log    /mnt/nostromo/www/manti.by/log/nginx_error.log;

    error_page 400 401 403 404 /lost/;
    error_page 500 502 503 504 /offline/;

    location /lost/ {
        alias /home/manti/www/manti.by/templates/static/400.html;

        expires never;
        access_log off;

        add_header Pragma 'public';
        add_header Cache-Control 'public, must-revalidate, proxy-revalidate';
    }

      location /offline/ {
        alias /home/manti/www/manti.by/templates/static/500.html;

        expires never;
        access_log off;

        add_header Pragma 'public';
        add_header Cache-Control 'public, must-revalidate, proxy-revalidate';
    }

    location /robots.txt {
        alias /home/manti/www/manti.by/static/robots.txt;

        expires 30d;
        access_log off;

        add_header Pragma 'public';
        add_header Cache-Control 'public, must-revalidate, proxy-revalidate';
    }

    location /manifest.webmanifest {
        alias /home/manti/www/manti.by/static/manifest.webmanifest;

        expires 30d;
        access_log off;

        add_header Pragma 'public';
        add_header Cache-Control 'public, must-revalidate, proxy-revalidate';
    }

    location /api/ {
        expires max;
        add_header Pragma 'public';
        add_header Cache-Control 'public, must-revalidate, proxy-revalidate';

        proxy_pass  http://manti-by;
        include     /home/manti/www/manti.by/config/params.conf;
    }

    location /dashboard/ {
        expires    off;
        add_header Cache-Control 'private';

        proxy_pass  http://manti-by;
        include     /home/manti/www/manti.by/config/params.conf;
    }

    location /static/ {
        alias /home/manti/www/manti.by/static/;

        if ($request_filename ~ "^.*/(.+\.(jpe?g|png|gif|svg|json|webp|js|css|eot|ttf|woff|woff2))$") {
            expires max;
            access_log off;

            add_header Pragma 'public';
            add_header Cache-Control 'public, must-revalidate, proxy-revalidate';
            add_header Access-Control-Allow-Origin *;
            break;
        }

        return 302 https://manti.by$request_uri;
    }

    location /content/ {
        alias /mnt/nostromo/www/manti.by/content/;

        if ($request_filename ~ "^.*/(.+\.mp3|flac)$") {
            set $fname $1;
            add_header Content-Disposition 'attachment; filename="$fname"';
        }

        if ($request_filename ~ "^.*/(.+\.(jpe?g|webp|mp3|ogg|flac|zip))$") {
            expires max;
            access_log off;

            add_header Pragma 'public';
            add_header Cache-Control 'public, must-revalidate, proxy-revalidate';
            add_header Access-Control-Allow-Origin *;
            break;
        }

        return 302 https://manti.by$request_uri;
    }

    rewrite /resume/ /about/ permanent;
    rewrite /copyrights/ /about/ permanent;

    location / {
        expires 7d;
        add_header Pragma 'public';
        add_header Cache-Control 'public, must-revalidate, proxy-revalidate';

        add_header Content-Security-Policy "default-src https:; style-src 'unsafe-inline' https://manti.by; script-src 'unsafe-inline' https://manti.by; media-src https://manti.by; frame-ancestors 'none';";
        add_header Strict-Transport-Security 'max-age=31536000; includeSubDomains; preload';
        add_header Referrer-Policy 'same-origin';

        add_header X-XSS-Protection '1; mode=block';
        add_header X-Frame-Options 'DENY';
        add_header X-Content-Type-Options 'nosniff' always;

        proxy_pass  http://manti-by;
        include     /home/manti/www/manti.by/config/proxy-params.conf;
    }

    ssl_certificate /etc/letsencrypt/live/manti.by/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/manti.by/privkey.pem; # managed by Certbot

    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}
